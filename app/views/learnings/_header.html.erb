      <div class="container bg-light sticky-top mt-1 pt-1 pb-1 border-bottom">
        <div class="row justify-content-md-center">
          <div class="col col-lg-3 d-flex align-items-center pt-1">
            <h5>Learning Tips : Chat Room</h5>
          </div>
          <div class="col col-lg-2 d-flex align-items-center pt-1">
            <h5><%= link_to '新規投稿', new_learning_path, class: "nav-template" %></h5>
          </div>
          <div class="col-md-auto d-flex align-items-center">
            <%= form_with(url: search_learnings_path, local: true, method: :get, class: "d-flex") do |form| %>
              <%= form.text_field :keyword, placeholder: "タイトルを検索する", class: "form-control me-2" %>
              <%= form.submit "検索", class: "btn btn-outline-success" %>
            <% end %>
          </div>
        </div>
      </div>